"""Pytest configuration for peval_gold."""

# Custom markers (registered in pyproject.toml [tool.pytest.ini_options])
# are documented here for IDE / discoverability:
#   slow    — encoder-load heavy tests (e.g., all-mpnet-base-v2)
#   network — requires Hugging Face dataset network access
